spring:
  application:
    name: mybiblestudyservice
  profiles:
    active: default, ${SPRING_PROFILE}
  cloud:
    config:
      enabled: true
      uri: ${CONFIGSERVER_URI}
    #uri: http://localhost:8888