spring.datasource.driver-class-name=${PSQLDBDRIVER}
spring.datasource.url=${PSQLDBURL}
spring.datasource.username=${PSQLDBUSER}
spring.datasource.password=${PSQLDBPASS}
management.endpoints.web.exposure.include=*